// IGA/Config/Debug.xcconfig
//
// Debug build configuration
// Include secrets (which should be gitignored)

#include? "Secrets.xcconfig"

// Note: Secrets.xcconfig provides actual values
// Fallbacks below only used when Secrets.xcconfig is missing
